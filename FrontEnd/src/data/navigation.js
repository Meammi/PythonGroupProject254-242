import { Map } from 'lucide-react';

/**
 * Navigation items for the sidebar.
 * Simplified to Map-only for a minimalist "Map-First" dashboard.
 */
export const navigationItems = [
  { id: 'map', icon: Map, label: 'Campus Map' },
];
